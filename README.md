# adana-cli

Command-line tools for [adana].

```sh
adana \
  --check \
  --file coverage/coverage.json \
  --require line 100 \
  --require statement 100 \
  --require function 100 \
  --without ignore
```
